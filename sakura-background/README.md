# React component library for Sakura Background

To compile the library and launch the demo, please execute these commands:

```bash
cd lib
npm install
npm run build
cd ../doc
npm install
npm start
```
